g.V().hasLabel('Person').has('id',$personId).out('knows').in('hasCreator').hasLabel('Post').has('creationDate',inside($startDate,$endDate)).out('hasTag').filter(__.in('hasTag').has('createDate', lt($startDate)).count().is(0)).groupCount().unfold().order().by(select(values),decr).by(select(keys).values('name')).limit(10)
