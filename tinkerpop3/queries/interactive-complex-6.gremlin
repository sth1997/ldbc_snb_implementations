g.V().hasLabel('Person').has('id', 30786325583618).both('knows').union(identity(), both('knows')).dedup().has('id', neq(30786325583618)).in('hasCreator').hasLabel('Post').filter(out('hasTag').values('name').fold().filter(Lists.containsAll(Lists.of('Angola')))).out('hasTag').has('name', neq('Angola')).groupCount().unfold().order().by(select(values), decr).by(select(keys).values('name'), incr).limit(10)

key shi tag(vertex), value shi count
