g.V().hasLabel('Person').has('id', $personId).in('hasCreator').as('post').in('replyOf').hasLabel('Comment').as('comment').order().by('creationDate', decr).by('id', incr).limit(20).out('hasCreator').as('commenter').select('commenter','comment')