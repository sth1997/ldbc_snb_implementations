g.V().hasLabel('Person').has('id', 24189255818757).in('hasCreator').as('post').in('replyOf').hasLabel('Comment').as('comment').order().by('creationDate', decr).by('id', incr).limit(20).out('hasCreator').as('commenter').select('commenter','comment')

key shi string(commenter, comment), value shi vertex
