g.V().hasLabel('Person').has('id', $personId).out('knows').union(identity(), out('knows')).dedup().by('id').has('id', neq($personId)).as('p').inE('hasMember').has('joinDate', gt($minDate)).outV().as('f').group().by(valueMap('id', 'title')).by(out('containerOf').hasLabel('Post').out('hasCreator').where(eq('p')).by('id').count()).unfold().order().by(select(values), decr).by(select(keys).select('id'), incr).limit(20)